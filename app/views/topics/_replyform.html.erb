<div id="divform">
<%= form_for(@comment) do |f| %>
	  <%= f.text_area :body %>
	  <%= f.hidden_field :topic_id, value: @topic.id %>
	  <%= f.hidden_field :reply_to, value: comment.id %>
	  <%= f.submit "Responder" %>
<% end %>
</div>